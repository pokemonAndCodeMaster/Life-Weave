已完成只读核查与方案，未修改文件，尚未完成追加后的验收。

- **用户可见行为**：保留 README 原有三行，在第 4 行新增 `Smoke: codex-guard`。
- **影响范围**：仅 `README.md`；不涉及程序模块、接口或依赖。
- **实现顺序**：进入实施阶段后，确认文件未发生变化，再在末尾追加目标行及换行符，检查差异仅包含该新增行。
- **风险**：避免重复追加、覆盖原文或改变换行格式。当前文件以 LF 换行结束，可直接追加。
- **验证**：执行 `grep -nF 'Smoke: codex-guard' README.md`，预期输出 `4:Smoke: codex-guard`，退出码为 0；结合 `git diff -- README.md` 确认原内容保留，并核对只有 README 被修改。
- **知识变化**：仅 README 增加冒烟标记，无需修改其他知识文件。仓库未提供 `scripts/lifeweave.py` 或已选项目知识正文，本轮未进行知识登记。

实际证据：基线提交为 `e80b6591cd8114e8129682e9a9706424132aa58b`；当前 README 差异为空。已运行上述 grep，结果无输出、退出码为 1，证明标记目前尚不存在。工作区已有未跟踪的 `.agents/`、`.lifeweave/`，本轮未改动。

本轮纠偏反馈为空，目标与范围保持不变。

**自检**：遵守本轮“只能阅读、不能修改文件”的要求；未将方案或基线检查表述为开发完成。

尚未解决的问题：实施阶段仍需实际追加并完成验证；当前证据尚不能证明只读阶段之后可完成开发，也不代表事项已获业务接受。
