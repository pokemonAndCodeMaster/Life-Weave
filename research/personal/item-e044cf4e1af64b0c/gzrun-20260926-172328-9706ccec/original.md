只读方案已完成，未修改文件。已核对提交 `af6d0728ef8d523090e86e36dc4777b82378886a` 的源码；`docs/development.md`、`docs/plugin-system.md` 的 SHA-256 均与本轮选入版本一致。

建议在 `docs/development.md` 的“开发 Agent 的受管委托”后新增“在网页中使用当前项目知识开发”，按以下步骤编写。

1. **从需求事项进入。** 在同一空间打开需求事项，进入“开发 Agent”。可先在“知识与材料”的“知识范围”选择“LifeWeave 项目”，阅读当前正文及页末版本；这里选择阅读文章不会改变开发委托输入。

2. **确认带入的知识再委托。** 填写“这次要交付什么”，核对“项目 Git 目录”、固定方法及默认项目知识数量。当前页面没有逐篇知识选择框：保持推荐目录时默认带入产品、架构、状态、开发维护四篇；改成其他目录后默认知识数量为零。不得把通用委托的材料选择操作写成开发页已有功能。核对未提交改动排除提示，选择方案检查方式，再点击“开始开发委托”。

3. **确认固定版本。** 在“本事项的开发委托”中查看 Git 提交和上下文版本，展开“固定输入与版本”，逐项核对方法、知识来源路径与版本摘要。页面显示的是哈希前 12 位；需要完整值时，核对开发委托接口返回的 `repositoryRevision`、`inputVersions`。项目提交与知识正文版本分别固定，不能假定二者来自同一提交。

4. **区分推荐、读取和执行。** 展开“插件计划与实际调用”，按下表解释证据：

   | 页面记录 | 可以确认的事实 |
   | --- | --- |
   | `lifeweave.knowledge / recommend` | 服务执行了候选推荐，不代表候选全部选入 |
   | `lifeweave.knowledge / read` | 服务读取了正文并形成输入快照 |
   | 方法插件 `/ bind` | 方法版本已固定，不证明模型遵循全部步骤 |
   | `lifeweave.execution.codex / run` 及开始事件 | 执行节点进入执行适配器调用边界 |

   页面没有独立的“已发送”确认状态。文档应说明：材料固定、写入执行目录、进入执行器调用是不同证据；均不能证明模型完整阅读或遵循。尚无调用、条件步骤、记录截断和结束未观测须保留原义。

5. **查看运行与检查。** 从调用中的“查看 Run”查看阶段、原生会话和事件；需要正文及原始记录时，展开同一委托的“方案”“独立审阅”“实施与验证”。`verify_readonly` 检查只读阶段工作树，`verify_inputs` 检查输入未变化，两者不等于业务测试通过。实施完成后查看“实际 Git 差异”，结合结果正文与实际命令判断验收；`awaiting_acceptance` 不代表已合并或业务接受。自检模式没有独立审阅 Run。

6. **核对同一篇知识的 Notion 镜像。** 根据固定输入中的来源路径，在同一空间重新打开对应项目知识，查看正文下方“Notion 镜像”。当前开发事项页没有内嵌镜像状态。
   - **未配置：** 页面提示后端未配置，发布按钮禁用；Codex 的交互授权不能替代后台凭据。
   - **已配置：** 只表示启用且配置了凭据文件、根页面，不代表每篇发布成功。
   - **已确认：** 查看“上次回读确认”的源版本是否与当前正文相同，同时检查最近失败提示。旧确认不能代表新版本已同步，也不能掩盖最近回读失败。
   - 已配置时，“发布并核对此篇”会检查当前源版本及 GitHub main 上的已提交正文，再发布并完整回读。配置和汇总状态入口为“设置与连接 → Notion 知识镜像”。本次不执行配置或发布。

受影响的实现只作为核对依据，不修改：`DevelopmentPanel.vue`、`PluginProcess.vue`、`KnowledgeNotionMirror.vue`、`NotionMirrorSettings.vue`，以及开发、运行、插件和镜像服务。相关接口包括开发选项/委托记录、`plugin-process`、Run/事件，以及 `notion-mirror/settings`、`status`、`document`；不新增接口。

实施顺序为：补写上述操作说明；在 `docs/README.md` 增加直达章节链接；用既有脚本同步生成 `docs/complete-guide.md`，随后执行：

```bash
python scripts/build_complete_guide.py
python scripts/build_complete_guide.py --check
git diff --check
```

`complete-guide.md` 是必要的派生文档同步；若修改分篇却不重建，完整汇编检查会失败。无需修改产品代码、当前来源清单或历史证据，也无需为纯说明变更新增测试。

本轮已实际运行 `python scripts/build_complete_guide.py --check`，结果为：**10 份当前来源全文一致，文档本地链接有效**。这是修改前基线检查。实施后还须逐项核对页面名称、链接及生成差异。

自检：已覆盖用户操作、模块与接口、实施顺序、风险、验证和知识变化；本轮纠偏反馈为空，没有额外改变目标或权限。

尚未解决的问题：当前页面无法逐篇选择开发知识，也不能在事项页直接查看 Notion 镜像；本方案如实说明这些限制。沙箱阻断本机 API，未完成在线页面验证、知识入口回读或后台授权状态核验。文档尚未落盘，修改后的汇编检查留待实施阶段完成；本轮结果不表示事项已被业务接受。